## Summary

<!-- What does this PR do, and why? -->

<!--
RELEASE NOTE — public-facing repos only (api, app, agent, plugin-sdk, connectors/*).

The public changelog uses your cleaned PR title by default. To override the wording, add a
fenced block anywhere in this body:

   ```release-note
   One user-facing sentence.
   ```

Category (Feature / Fix / Breaking) comes from your conventional-commit title — and that
title is also how you keep a change OUT of the changelog: type non-user-facing work as
chore/refactor/docs/build (the kernel drops those), and remember the public site shows
Features + Breaking only.
See https://portal.infusal.io/contributing/changelog-notes/
-->
