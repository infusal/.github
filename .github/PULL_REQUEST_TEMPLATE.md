## Summary

<!-- What does this PR do, and why? -->

<!--
RELEASE NOTE — public-facing repos only (api, app, agent, plugin-sdk, connectors/*).

The public changelog uses your cleaned PR title by default. Two optional controls:

1. Override the wording — add a fenced block anywhere in this body:

   ```release-note
   One user-facing sentence.
   ```

2. Omit this PR from the changelog — add the `skip-changelog` label.

Category (Feature / Fix / Breaking) always comes from your conventional-commit title.
See https://portal.infusal.io/contributing/changelog-notes/
-->
